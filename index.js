for(let contador = 1 ; contador < 6 ; contador++) {
  console.log(contador);
}

for (let i = 5; i > 0; i--) {
  console.log(i)
}

const prompt = require('prompt-sync');
const entrada = prompt();

var quantosNumerosSeraoSomados = entrada('Digite aqui quantos números desejam somar: ')